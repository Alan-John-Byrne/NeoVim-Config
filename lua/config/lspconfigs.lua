-- IMPORTANT: Java's LSP (nvim-jdtls) works independently from nvim-lspconfig. NOT INCLUDED HERE.

-- NOTE: EXAMPLE
-- local lspconfig = require("lspconfig")
-- lspconfig.gopls.setup({
-- settings = {
-- *settings got to do with the golang lsp*
-- }
